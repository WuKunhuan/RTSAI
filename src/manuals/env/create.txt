Usage: RTSAI env create <env_name>

Description:
The `create` command allows you to create a new RTSAI environment. Provide a unique name for the environment as the `<env_name>` argument.

Example:
RTSAI env create my_environment