import os, pandas

class RTSAI_graph(): 

    # Create a graph as the specified name in the path, 
    # having specified entries, and have initial rows (default: [])
    def __init__ (self, path, columns, entries = []): 
        pass

# This function copies the existing graphs and 
# renames the graph files with name prefix
def RTSAI_graph_setup(): 
    pass

# import grammatical rules from the knowledge graph
def Natural_Language_Processing(): 
    pass

